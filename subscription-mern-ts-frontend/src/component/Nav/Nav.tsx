import { Navbar, NavItem, NavLink } from 'react-bootstrap'

function Nav() {
    return (
        <Navbar>
            <NavItem>
                <NavLink>Home</NavLink>
            </NavItem>
        </Navbar>
    )
}

export default Nav